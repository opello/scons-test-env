Import('env')

#env.VariantDir('generator', '#build/generator', duplicate=0)

srcFiles = [
   'main.cpp',
   #'generator/soapC.cpp',
   '#build/generator/soapC.cpp',
]

env.Program( 'a.out', srcFiles )
